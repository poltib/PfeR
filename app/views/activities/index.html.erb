<h1>Friend's Activities</h1>

<ul>
<% @activities.each do |activity| %>
	<li>
    <%= link_to activity.owner.username, user_path(activity.owner) if activity.owner %>
    <%= render_activity activity %>
  </li>
<% end %>
</ul>