<section>
  <div class="container">
  <h2>Utilisateurs qui aiment <%= link_to @favoritable.name, @favoritable %></h1>

  <ul>
    <% @favorites.each do |favorite| %>
      <li>
        <%= link_to favorite.user.username, favorite.user %>
      </li>
    <% end %>
  </ul>

  </div>
</section>
