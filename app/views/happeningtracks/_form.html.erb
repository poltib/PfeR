<%= form_for ([@happening, @happening.tracks.build]) , :html => { :multipart => true, :class => 'create' } do |f| %>
  <% if @track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@track.errors.count, "error") %> prohibited this track from being saved:</h2>

      <ul>
      <% @track.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
  <ul>
    <li><%= f.label :name %>
  <%= f.text_field :name %></li>
  <li id="jsRoute"><%= f.hidden_field :route %></li>
  <li id="upRoute"><%= f.label :route %>
  <%= f.file_field :route %></li>
  </ul>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>